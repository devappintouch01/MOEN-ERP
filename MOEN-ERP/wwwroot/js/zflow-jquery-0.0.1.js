// Define the zFlowDesignView method
/*
function zFlowDesignView() {
    // Create or manipulate the div with ID zFlowDesign
    $('#zFlowDesign').html('<h1>Content generated by zFlowDesignView()</h1>');
}

// Ensure the jQuery code runs when the document is ready
$(document).ready(function () {
    // Call zFlowDesignView() when the document is ready
    zFlowDesignView();
});
*/

document.addEventListener('DOMContentLoaded', (event) => {
    // Create the modal element
    var modalHtml = `
    <div id="ssmodel" class="modal">
      <div class="modal-content">
        <span class="close">&times;</span>
        <h2>Modal Title</h2>
        <p>This is a custom modal.</p>
        <button id="confirmBtn">Confirm</button>
        <button id="cancelBtn">Cancel</button>
      </div>
    </div>
  `;

    // Append the modal to the body
    document.body.insertAdjacentHTML('beforeend', modalHtml);

    // Get the modal
    var modal = document.getElementById("ssmodel");

    // Get the button that opens the modal
    var btn = document.getElementById("openFlowBtn");

    // Get the <span> element that closes the modal
    var span = document.getElementsByClassName("close")[0];

    // Get the confirm and cancel buttons
    var confirmBtn = document.getElementById("confirmBtn");
    var cancelBtn = document.getElementById("cancelBtn");

    // When the user clicks the button, open the modal 
    btn.onclick = function () {
        modal.style.display = "block";
    }

    // When the user clicks on <span> (x), close the modal
    span.onclick = function () {
        modal.style.display = "none";
    }

    // When the user clicks the confirm button, do something and close the modal
    confirmBtn.onclick = function () {
        alert("Confirmed!");
        modal.style.display = "none";
    }

    // When the user clicks the cancel button, close the modal
    cancelBtn.onclick = function () {
        modal.style.display = "none";
    }

    // When the user clicks anywhere outside of the modal, close it
    window.onclick = function (event) {
        if (event.target == modal) {
            modal.style.display = "none";
        }
    }
});